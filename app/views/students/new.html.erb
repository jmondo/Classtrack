<%= simple_form_for @student, html: { class: 'form-horizontal' } do |f| %>
  <%= f.input :email, required: false %>
  <%= f.input :course_tokens, required: false %>
  <%= f.submit "Start tracking" %>
<% end %>
